//Arithmatic operators are used to perform mathematical operations on numbers. 
// They include addition (+), subtraction (-), multiplication (*), division (/), 
// modulus (%), and exponentiation (**).

let a = 10, b = 3;   //assign the value first to a and b
console.log(a);
console.log(b);

let sum = a + b; // addition
let sub = a - b; // subtraction
let mul = a * b; // multiplication
let div = a / b; // division

console.log(sum);
console.log(sub);
console.log(mul);
console.log(div);

