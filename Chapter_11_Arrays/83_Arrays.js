
let fruits = []; // []- empty array
let fruits_fresh = ["apple", "banana", "cherry"];// length=3 and indexes=0,1,2



//Example:

let arr = [10, 20, 30, 40]; // arr- integers array // length=4 and indexes=0-3

console.log(arr[0]); // solution: 10 ----//we can access the functions directly
console.log(arr[3]);// solution: 40
console.log(arr[4]);// undefined ---// it is not defined

// it doesn't shows error for ------------> console.log(arr[4]);
// in java it shows error or exception
// //in python index out of range or bound exception
