// What is the size of the matrix:

let matrix_2D = [
    [1, 2, 3, 4],   // size: 1x4 - 1 row and 4 column
];


//Example of perfect/symmetric/square matrix or a grid

let grid = [
    [10, 20, 30],
    [40, 50, 60],
    [70, 80, 90]
];  //nxn- ---> 3x3 matrix is a are known as above decription as it has same rows and same columns

console.log(grid[0][0]);   //sol: 10 ---> We can access them by using the indexes (indexations) also
console.log(grid[0][2]);  //sol: 30
console.log(grid.length); // sol: 3 ----> length of grid will be always no. of rows
console.log(grid[0].length); // sol: 3 as it is a 3x3 matrix
