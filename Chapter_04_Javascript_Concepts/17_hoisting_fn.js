function getUserStatus() {
    //var status_code; JS Engine (optimize the code) (In typescript it is not allowed)
    console.log(status_code);
    var status_code = "Active";
    console.log(status_code);
}

getUserStatus();

//Note: var is function scoped, so it is hoisted to the top of getUserStatus(), NOT the global scope.