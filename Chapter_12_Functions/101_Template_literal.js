function greet(name) {
    return `Hello,${name}`;
}
let result = greet(" Alice"); //In this type we need to save the result, then only the value will be printed
console.log(result);


//doubt: console .log just print the line but return will send the value also